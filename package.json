{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "concurrently --kill-others \"HOST=0.0.0.0 nuxt dev\" \"vitest\"",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest"
  },
  "stackblitz": {
    "startCommand": "npm run dev"
  },
  "devDependencies": {
    "@types/node": "^18",
    "concurrently": "^8.0.1",
    "nuxt": "^3.4.3"
  },
  "dependencies": {
    "@nuxt/test-utils": "^3.4.3",
    "vitest": "^0.30.1"
  }
}
